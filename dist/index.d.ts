import React from 'react';

interface ButtonProps$1 {
    label: string;
}
declare const Button: (props: ButtonProps$1) => React.JSX.Element;

interface ButtonProps {
    label: string;
}
declare const Button2: (props: ButtonProps) => React.JSX.Element;

export { Button, Button2 };
